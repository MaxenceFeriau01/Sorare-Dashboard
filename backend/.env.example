# ========================================
# SORARE DASHBOARD - Configuration
# ========================================

# Database (Utilisez des variables séparées pour une meilleure configuration)
# ⚠️ Remarque : La variable DATABASE_URL est prioritaire si elle est complète.
POSTGRES_USER=sorare # Exemple du nom d'utilisateur dédié
POSTGRES_PASSWORD=sorare_super_secret_password_2024 # Exemple de mot de passe fort
POSTGRES_DB=sorare_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_URL=redis://localhost:6379/0

# Sorare API (en attente de l'accès officiel)
SORARE_API_URL=https://api.sorare.com/graphql
SORARE_EMAIL=ton_email@example.com
SORARE_PASSWORD=ton_mot_de_passe_sorare

# ========================================
# TWITTER/X CREDENTIALS (pour le scraping)
# ========================================
TWITTER_EMAIL=ton_email@example.com
TWITTER_PASSWORD=ton_mot_de_passe_twitter
TWITTER_USERNAME=ton_@username
TWITTER_BEARER_TOKEN=ton_token_bearer_twitter_si_utilise

# ========================================
# SCRAPING SETTINGS
# ========================================
SCRAPING_ENABLED=true
SCRAPING_INTERVAL_MINUTES=60
SCRAPING_RATE_LIMIT_SECONDS=2
SCRAPING_DELAY_MIN=5
SCRAPING_DELAY_MAX=15
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# ========================================
# CELERY (tâches automatiques)
# ========================================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
UPDATE_PLAYERS_INTERVAL=60
UPDATE_INJURIES_INTERVAL=30
UPDATE_TWITTER_INTERVAL=15

# ========================================
# BACKEND API & ENV CONFIGURATION (Variables manquantes)
# ========================================
API_SECRET_KEY=change_this_to_a_random_secret_key_min_32_chars
API_ALGORITHM=HS256
API_ACCESS_TOKEN_EXPIRE_MINUTES=30
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Sorare Dashboard

# ✅ AJOUTÉ pour résoudre l'AttributeError
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=INFO
TIMEZONE=Europe/Paris